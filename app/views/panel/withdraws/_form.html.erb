<%= form_with(model: [:panel, withdraw], local: true) do |form| %>
  <% if withdraw.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(withdraw.errors.count, "error") %> prohibited this withdraw from being saved:</h2>

      <ul>
        <% withdraw.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :value %>
    <%= form.number_field :value, id: :order_value, class: 'form-control col-3' %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>