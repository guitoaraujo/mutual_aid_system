<%= form_with(model: [:panel, order], local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testis.errors.count, "error") %> prohibited this testis from being saved:</h2>

      <ul>
        <% order.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_type %>
    <%= form.select :order_type, Order.order_types.keys.map { |type| [type.humanize, type] }, id: :order_order_type %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value, id: :order_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>